CREATE TABLE IF NOT EXISTS tips (
    tip_id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    tip_total DECIMAL(19, 4) NOT NULL,
    tip_date TIMESTAMPTZ DEFAULT now(),
    user_id INTEGER NOT NULL REFERENCES users(user_id)
);